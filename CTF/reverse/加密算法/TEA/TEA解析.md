# TEA解析

## 本质

TEA本质就是一个简单的分组加密算法

**分组长度：64 bit（8 字节）**

**密钥长度：128 bit（16 字节）**

**结构：典型 Feistel 结构**——左右两部分轮流混合、交换

**轮数：64 轮（32 轮 Feistel，每轮两步）**

## 特征

1. 最明显的特征：有一个固定常数为0x9E3779B9（黄金分割比）
2. 两个 32bit 的块循环混合（v0, v1）
3. 反复出现的移位模式：<<4 与 >>5

